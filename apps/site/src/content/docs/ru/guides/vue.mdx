---
title: Vue-адаптер
description: Подключение XenolithGraph в Vue 3 через `<XenolithGraph>`, composables, in-editor панели и Vue SFC-виджеты.
---

:::caution[Перевод может отставать]
{/* EN-canonical-banner */}
Английская версия — канонический источник. Если что-то противоречит [английской странице](../../../guides/vue/) — доверяй ей. Переводы будем пересинхронизировать периодически до v1.0.
:::

См. [англоязычную версию](../../../guides/vue/) — там полное API адаптера `@xenolithengine/graph-vue`:
`<XenolithGraph @ready>`, composables (`useEditor`, `useEditorOrNull`, `useEditorReady`,
`useEditorEvent`, `useNodes` / `useEdges` / `useSelection` / `useViewport` / `useGraphJSON` /
`useUndoRedo`), in-editor компоненты (`<XenolithPanel>`, `<XenolithButton>`, `<XenolithControls>`,
`<XenolithMiniMap>`), `vueWidget(Component)` для Vue SFC-виджетов, и события через `@*-*` listeners.
